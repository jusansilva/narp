version: 0.2

phase:
  install:
    reuntime-versions:
      nodejs: 14

    commands:
      - npm install

  build:
    commands:
      - npm run-script build

  artifacts:
    files:
      - public/**/*
      - src/**/*
      - package.json
      - scripts/**/*
